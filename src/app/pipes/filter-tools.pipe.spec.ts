import { FilterToolsPipe } from './filter-tools.pipe';

describe('FilterToolsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterToolsPipe();
    expect(pipe).toBeTruthy();
  });
});
