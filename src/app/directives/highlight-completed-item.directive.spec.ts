import { HighlightCompletedItemDirective } from './highlight-completed-item.directive';

describe('HighlightCompletedItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedItemDirective();
    expect(directive).toBeTruthy();
  });
});
